NEXT_PUBLIC_TAMBO_API_KEY=api-key-here
# Optional override for the Tambo API base URL.
# When unset, `@tambo-ai/react` defaults to Tambo Cloud (https://api.tambo.ai).
# Must be a full URL including the protocol (e.g. https://api.tambo.ai).
# Uncomment and set this only if using a custom/self-hosted Tambo deployment.
# NEXT_PUBLIC_TAMBO_URL=https://your-self-hosted-tambo.example.com
